# Dashboard Foundation Plan

- [ ] `pending` Scaffold Next.js App Router project with strict TypeScript and package-manager lockfile.
- [ ] `pending` Establish environment validation and server/client configuration boundaries.
- [ ] `pending` Implement authentication, session refresh, permissions, and protected layouts.
- [ ] `pending` Establish semantic design tokens and accessible primitives.
- [ ] `pending` Generate a typed REST client from OpenAPI and establish normalized error handling.
- [ ] `pending` Implement one authenticated socket provider with reconnect, room restoration, health state, and REST reconciliation.
- [ ] `pending` Build typed feature-flag catalog, snapshot provider, route guards, and navigation filtering.
- [ ] `pending` Build authorized flag-management UI with audit reason, preview cohort, schedule, owner, and expiry.
- [ ] `pending` Add observability and safe operator-action audit context.
- [ ] `pending` Establish component and Playwright test harnesses.
- [ ] `pending` Deliver the first feature management vertical slice.

## Dashboard decisions to record

- Component/design-system foundation.
- REST/OpenAPI generation and HTTP client approach.
- Socket transport, event envelope, and reconnect policy.
- Authentication cookie/session boundary.
- Table and visualization libraries.
- Deployment/runtime model.
