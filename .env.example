# Host development reaches the containerized backend through its published port.
# Docker Compose overrides this with host.docker.internal inside the container.
# Never expose this value as NEXT_PUBLIC.
MIKOZI_API_URL=http://localhost:9289/api/v1
